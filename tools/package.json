{
  "name": "chapulin-devtools",
  "private": true,
  "description": "Dev tooling only (commitlint); the stack itself is C11 + libc, no dependencies.",
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2"
  },
  "license": "Apache-2.0"
}
