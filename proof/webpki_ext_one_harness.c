// The one-Extension part of the webpki_ext proof: read_one_extension
// judged from any reader state and any walk state, at the list bound
// CH_PROOF_ONE_LEN the launch line records. A proof name is one launch
// line, so the part gets its own file, the pem_ecdsa precedent; the
// body and what it proves are in webpki_ext_harness.c.
#define CH_PROOF_PARTS 8
#include "webpki_ext_harness.c"
