// The ECDSA-build variant of the x509der proof: same harness, the
// other SPKI arm and keyusage mask. A proof name is one launch line,
// so the variant gets its own file, the eeparse precedent.
#define CH_PIN_ECDSA 1
#include "x509der_harness.c"
