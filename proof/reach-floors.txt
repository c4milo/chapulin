# Reachability floors: the share of program locations each harness reaches
# at its configured bound, measured by `cbmc --cover location` under the
# same --unwind, --unwindset and -D flags its launch line proves under.
# The numbers are the nightly runner's (ubuntu, the pinned cbmc): a
# macOS build of the same harness carries a few more or fewer locations
# in its goto model, so a floor measured there can sit a few tenths of
# a percent off the runner's reading. Record what the nightly's own
# table prints, not a local measurement.
# proof/coverage.py builds that command from proof/run.sh: the launch
# line's own sources and flags, then the -D flags launch() adds to every
# harness. `python3 -u proof/coverage.py --reach --only NAME` measures one
# harness that way and prints the command it ran, so a floor recorded by
# hand and the nightly's number come from one command. From 6552715,
# which added -DCH_NATIVE_WIDEMUL to launch(), until
# https://github.com/c4milo/chapulin/issues/113 the cover command left
# that define out, so the nightly measured poly1305 on ct.h's 16x16
# decomposition and held it to a floor recorded on the native multiply
# its proof compiles. `--cover location` counts basic blocks, and each
# form of ct_widemul is one block, so poly1305 measured 149 of 158
# (94.3%) under both commands on 2026-09-02: the fix moved the command,
# not a floor.
#
# Why a floor rather than a threshold: a harness that stubs its dependencies
# legitimately reaches less of its translation unit than one that compiles
# them concretely, so there is no single number that is right for all of
# them. What is always wrong is a harness reaching less than it used to,
# because that means a bound shrank below the code the harness names and the
# proof still reports success. That is the failure this file exists to
# catch, and it is not hypothetical: 7206f4c and 93e9504 both shipped
# proofs that passed while proving less than they claimed.
#
# One "name percent" line per harness, and an optional budget=SECONDS field
# for a harness whose cover run needs longer than proof/coverage.py's
# REACH_BUDGET_S. A harness absent from this file is
# measured and reported but carries no floor; the nightly publishes the
# table, so adding a line is a matter of copying the number it printed.
# Lower a floor only with the reason in the commit body.
#
# See https://github.com/c4milo/chapulin/issues/55.

# Measured with cbmc 6.11.0, the version CI pins.
#
# certparse and hybrid_secret fell a tenth and three tenths on 2026-09-18
# because their goto models grew, not because the bound entered less. Both
# reach the same count they did at 4e9367a: certparse 89 locations, of 305
# then and 306 now; hybrid_secret 55, of 272 then and 277 now. What each
# gained is proved elsewhere. certparse gained parse_alpn, which
# handshake_parser.c declares beside the certificate parser and the
# eeparse_alpn launch line proves. hybrid_secret gained the five locations
# of the require_pq check at handshake.c:238-247, which the harness
# includes through handshake.c and does not enter; proof/key_share_harness.c
# proves that comparison and six test files drive it. The counts here come
# from each launch line's own --cover location command on arm64 macOS; the
# percentages below are the nightly's.
#
# hybrid_secret fell again, from 19.9 to 17.2, for the same reason. It
# reaches the same 55 locations, now of 320 rather than 277, and the same
# ones: main's 19, all 10 of hybrid_secret's, and the stubs'. 33978f6
# moved the flight handlers and hybrid_secret out of handshake.c into
# handshake_flight.c, and the harness includes both files. The split added
# 43 locations to the handlers and the driver, and the harness enters
# none of them. The handshake_psk and handshake_pin launch lines link
# handshake_flight.c and prove the handlers.

aead 97.4
aead_forge 96.9
aead_overlap 97.4
buf 90.2
certparse 29.1
certparse_webpki 26.3
chacha20 98.1
ct 94.1
drbg 84.4
# eeparse_webpki fell from 29.5 to 29.4 when handshake_parser.c gained
# parse_alpn: this harness drives the block with an empty ALPN offer, so
# that function's locations are in its goto model and out of its reach.
# eeparse_alpn is the harness that reaches them, and it carries its own
# floor below.
eeparse_webpki 29.4  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
eeparse_alpn 21.5  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
epoch 36.1
handshake_record 92.1
hello_build 76.9
hkdf 52.5
hybrid_secret 17.2
key_share 21.9
keysched 87.3
p256_mul 86.7
p384_mul 86.7  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
# pem and pem_ecdsa carry their own budget, 2700 s, where every other
# cover run gets 1800 s (https://github.com/c4milo/chapulin/issues/163).
# Their nightly cover runs took 1664 s and 1505 s on 2026-09-21, 1113 s
# and 986 s on 2026-09-22, and 1667 s and 1505 s on the morning of
# 2026-09-23, under the same command each night. The scheduled run later
# that day passed 1800 s on both and failed the check with no number, so
# the runner's own swing crosses the shared budget. 2700 s is the issue's
# estimate of what pem needs on a slow runner, and the proof-reach job's
# 300-minute limit holds two of them beside the rest of the roster.
pem 74.6 budget=2700
pem_ecdsa 74.6 budget=2700
poly1305 94.3
# quic_initial carries a floor where the other four TRANSPORT=quic
# harnesses carry none: its cover command converges in seconds, because
# every call it makes is a stub and the formula holds length checks
# alone.
quic_initial 98.7  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
# quic_packet is the one TRANSPORT=quic harness whose cover command
# converges: 34 s and 0.75 GB against the 1800 s budget, where quic_aes
# returned no number in 21 minutes. Its proof is cheap for the same
# reason -- ChaCha20 and the AEAD are stubs, so the formula holds
# framing rather than a keystream.
quic_packet 75.8  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
record 95.0
rsa_mul 32.3
rsa_mul_webpki 32.3  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
rsa_pkcs1 90.3  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
rsa_pkcs1_webpki 90.3  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
rsa_webpki 91.7  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
sha512 90.9  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
sha512_compress 97.1  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
softmul 91.3
webpki_cert 56.7  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
webpki_chain 80.6  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
webpki_ext 40.0  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
webpki_ext_one 51.4  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
webpki_ext_walk 54.8  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
webpki_name 24.2  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
webpki_san 29.8
webpki_sigalg 41.9  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
webpki_spki 41.1  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
webpki_time 33.3  # local reading, cbmc 6.11.0 on arm64 macOS; replace with the nightly table's number
x25519_tail 52.7

# srv_parser has no proof at all, not merely no cover floor: its own
# formula does not converge either, so nothing to measure exists yet.
# proof/run.sh states the split that would give it one.
# No floor, because `cbmc --cover location` does not converge on them.
# This is a limit of the measurement, not a gap someone forgot: the
# same solver cost that makes the proof slow makes counting its
# reachable locations slow. proof/coverage.py reads this block and
# skips them: running them anyway cost the nightly its runner (a cover
# formula grew past the VM's memory on 2026-09-02 and the job died
# without a log). Re-measure one by hand: `--only NAME` runs a harness
# named here under the cover command and REACH_BUDGET_S, and the command
# it prints runs under any budget. Move it to the roster above when it
# converges. Each got five minutes a harness, except handshake_pin and
# handshake_psk, which got ninety minutes each, under the command
# proof/coverage.py passed before
# https://github.com/c4milo/chapulin/issues/113 (cbmc 6.11.0, built-in
# solver, no -DCH_NATIVE_WIDEMUL). Of this block only mlkem_poly compiles
# a ct_widemul-family function, so that define changes its formula
# alone; the others were measured under the formula the cover command
# runs now.
# handshake_pin and handshake_psk were still in propositional reduction
# when the budget killed them, at 8.3 GB and 7.4 GB peak RSS.
# proof/coverage.py stops a harness at REACH_BUDGET_S, so the nightly
# reports both as timed out. hkdf_expand and hkdf_expand_label converge (60.0%
# and 81.2% on 2026-09-02, about 520 s each) but at 16 GB of memory,
# the whole of the nightly runner, which is the profile that killed the
# job; they carry no floor, so they are listed here rather than run.
# x25519_step was still in propositional reduction at 4.4 GB when the
# 900 s cap stopped it; x25519_tail, the same stub over four mul
# instances instead of ten, finishes in 436 s and carries a floor above.
# x25519_mul_ct is x25519_mul's lemma over the 16x16 decomposition; it
# carries the same 32 int64 products and timed out the same way under
# the cover command on 2026-09-03 (cbmc 6.11.0, 1.4 GB at the cap).
#
# The four TRANSPORT=quic harnesses are listed from one measurement and
# one inference, and the inference is why they are here rather than in
# the roster. quic_aes returned no usable number under the cover command
# on a development machine on 2026-09-18 (arm64 macOS, the pinned cbmc,
# about 21 minutes, 0.6 GB peak) -- and its proof takes 23 s, so cover
# costs it more than fifty times what proving it costs. quic_gcm_refusal
# was still running when the 1500 s cap on that whole session stopped it;
# quic_gcm_safety and quic_ghash were not reached. Their proofs take 282 s,
# 52 s and 274 s, all above quic_aes's 23 s, so nothing suggests their
# cover formulas do better than the one that already failed. Listing all
# four keeps the nightly from spending up to 1800 s each finding that out.
# Measure one by hand with `--only NAME` and move it to the roster above
# when it converges.
#
# mlkem_ntt, mlkem_invntt_low and mlkem_invntt_high timed out in all five
# nightly runs whose logs remain (33686419744, 33691163738, 33715874614,
# 33717153636, 33731054741), and the three of them took 45 minutes of
# every one of those runs. They carry no floor, so each timeout failed
# nothing. mlkem_ntt returned no verdict in 1800 s either, under the
# cover command on a development machine on 2026-09-03 (cbmc 6.11.0,
# 6.3 GB at the cap). That machine runs hello_build's cover in 190 s
# against the runner's 280, so 1800 s there buys more solver time than
# 1800 s on the runner would. The other two are the same butterfly over
# the other half of the transform. Their proofs are unaffected: run.sh
# proves all three, and prove-slow reports the three of them green.
#
# pem and pem_ecdsa timed out in those same five runs, at a budget below
# what they need rather than because they do not converge. Under
# REACH_BUDGET_S they do: run 33888303774 measured pem at 74.6% in 1665 s
# and pem_ecdsa at 74.6% in 1503 s, and both carry floors above.
#
# Their runner times also correct a wrong inference. pem takes 1654 s on a
# development machine and 1665 s on the runner, a ratio of 1.01, while
# hello_build takes 186 s there and 310 s here, a ratio of 1.67. The two
# machines do not differ by one factor. hello_build peaks at 12.2 GB
# against the runner's 16 GB and pays for it; pem peaks at 8.1 GB and does
# not. Scale one harness's time by another harness's ratio and the answer
# is wrong by 800 s, which is how a prediction that pem would still time
# out survived until this run measured it.
#
# hello_build_webpki converges, and it is listed here for the reason
# hkdf_expand is: the cost. Under the cover command on a development
# machine on 2026-09-15 (cbmc 6.11.0) it reached 78.8% in 1681 s of the
# 1800 s budget, and 596 s of that was system time, the profile of a
# machine paging. That machine runs hello_build's cover in 186 s against
# the runner's 310, so the runner would reach the budget and report a
# timeout. A floor plus a timeout fails the check, so this harness carries
# no floor and is skipped rather than run. hello_build, the same builder
# without the server_name extension and the five signature schemes,
# has a floor recorded above at 76.9%.
#
# p384 is p256's harness at twelve limbs and times out the same way:
# under the cover command on a development machine on 2026-09-15
# (cbmc 6.11.0, -DCH_NATIVE_WIDEMUL) it was still in propositional
# reduction at 4.6 GB when the 1800 s cap stopped it. p384_mul, the
# same stub over the field multiply alone, converges in seconds and
# carries a floor above.
#
#   eeparse
#   handshake_parser
#   handshake_pin
#   handshake_post
#   handshake_psk
#   hello_build_webpki
#   hkdf_expand
#   hkdf_expand_label
#   io
#   mlkem
#   mlkem_invntt_high
#   mlkem_invntt_low
#   mlkem_ntt
#   mlkem_poly
#   p256
#   p384
#   quic_aes
#   quic_gcm_refusal
#   quic_gcm_safety
#   quic_ghash
#   rsa
#   sha256
#   sha3
#   sha3_stream
#   x25519_mul
#   x25519_mul_ct
#   x25519_step
