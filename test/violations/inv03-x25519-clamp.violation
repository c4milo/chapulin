invariant: INV-3
file: x25519.c
catches: diff
builds: bin/diff
reason: dropping the RFC 7748 cofactor clamp lets a scalar leave the
  prime-order subgroup, so a small-subgroup point leaks key bits

--- old
    z[0] &= 248;
--- new
    z[0] &= 255;
